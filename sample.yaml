namespace: sample
sample:
  defines: runnable
  metadata:
    name: sample
    description: Sample Java Application
    icon: https://emojiapi.dev/api/v1/bookmark.svg
  containers:
    sample:
      build: .
  variables:
    java-version:
      env: JAVA_VERSION
      type: string
      description: Java version used in the project
      value: '1.8'
